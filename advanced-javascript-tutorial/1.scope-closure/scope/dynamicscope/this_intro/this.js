/*
Every function, while executing, has a reference to its current execution context, called this.
(Javascript's version of "dynamic scope" is this)

Execution context: 
    - where the function is called.
    - how the function is called. 
    - when the function is called.

this keyword tell the engine where it needs to search for the scope of the variables being referred in current context.

4 rules hwo this keyword gets bound (precedence of rule is also imp)
(all rules depend on the call site, where function being called).
*/

